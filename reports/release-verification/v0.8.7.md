# リリース確認レポート: v0.8.7

> 総合結果: **✅ 全ステップ成功**

v0.8.7 は、crates.io / PyPI に公開されたパッケージを
そのままインストールした状態で、ライブラリ・組み込み(ファイル)・
サーバー・クラスタのすべてが同一データに対して同一の結果を返すことを
確認済みである。

## ステップ

### 1. コンテナイメージビルド ✅

検証専用の Docker イメージをビルドする。alopex-cli/alopex-server は `cargo install`、alopex(Python) は `pip install` で crates.io/PyPI から取得する(このイメージには alopex のソースコードを一切 COPY しない)。

```
#20 1.751   Downloaded thiserror v1.0.69
#20 2.509   Downloaded thiserror-impl v2.0.17
#20 2.511   Downloaded thiserror-impl v1.0.69
#20 2.512   Downloaded thiserror v2.0.17
#20 44.70    Compiling thiserror v1.0.69
#20 56.90    Compiling thiserror-impl v1.0.69
#20 93.02    Compiling thiserror v2.0.17
#20 101.7    Compiling thiserror-impl v2.0.17
#20 375.3   Downloaded serde_path_to_error v0.1.20
#20 394.7    Compiling thiserror v1.0.69
#20 402.5    Compiling thiserror-impl v1.0.69
#20 433.9    Compiling thiserror v2.0.17
#20 446.5    Compiling thiserror-impl v2.0.17
#20 464.7    Compiling serde_path_to_error v0.1.20
#20 488.0    Compiling futures v0.3.31
#20 488.0    Compiling tonic-prost-build v0.14.6
#20 488.4    Compiling ordered-multimap v0.7.3
#20 488.7    Compiling pest_derive v2.8.5
#20 489.2    Compiling md-5 v0.10.6
#20 489.3    Compiling hyper-timeout v0.5.2
#20 489.5    Compiling protobuf-support v3.7.2
#20 490.7    Compiling tokio-stream v0.1.18
#20 491.1    Compiling hashlink v0.8.4
#20 491.2    Compiling encoding_rs v0.8.35
#20 491.4    Compiling base64 v0.21.7
#20 491.9    Compiling arraydeque v0.5.1
#20 492.1    Compiling unicode-segmentation v1.12.0
#20 492.7    Compiling hex v0.4.3
#20 492.9    Compiling prometheus v0.14.0
#20 493.0    Compiling minimal-lexical v0.2.1
#20 493.3    Compiling nom v7.1.3
#20 496.8    Compiling convert_case v0.6.0
#20 498.1    Compiling yaml-rust2 v0.8.1
#20 502.8    Compiling ron v0.8.1
#20 505.6    Compiling tonic v0.14.6
#20 518.5    Compiling arc-swap v1.8.0
#20 518.8    Compiling json5 v0.4.1
#20 521.9    Compiling rust-ini v0.20.0
#20 523.5    Compiling alopex-server v0.8.7
#20 524.4    Compiling tokio-rustls v0.26.4
#20 524.7    Compiling toml v0.8.23
#20 528.0    Compiling sharded-slab v0.1.7
#20 528.6    Compiling matchers v0.2.0
#20 528.7    Compiling tracing-log v0.2.0
#20 529.0    Compiling thread_local v1.1.9
#20 529.3    Compiling pathdiff v0.2.3
#20 529.5    Compiling humantime v2.3.0
#20 529.9    Compiling nu-ansi-term v0.50.3
#20 530.4    Compiling tracing-subscriber v0.3.22
#20 535.7    Compiling humantime-serde v1.1.1
#20 535.8    Compiling axum-server v0.8.0
#20 537.2    Compiling config v0.14.1
#20 545.6    Compiling tonic-prost v0.14.6
#20 545.7    Compiling alopex-cluster v0.8.7
#20 549.3    Compiling tower-http v0.6.8
#20 549.6    Compiling rustls-pemfile v2.2.0
#20 549.8    Compiling dashmap v5.5.3
#20 587.2     Finished `release` profile [optimized] target(s) in 3m 32s
#20 587.2   Installing /home/verify/.cargo/bin/alopex-server
#20 587.2    Installed package `alopex-server v0.8.7` (executable `alopex-server`)
#20 DONE 587.7s

#21 [stage-1 10/11] COPY --chmod=0755 docker-entrypoint.sh /usr/local/bin/docker-entrypoint.sh
#21 DONE 0.0s

#22 [stage-1 11/11] WORKDIR /workspace
#22 DONE 0.0s

#23 exporting to image
#23 exporting layers
#23 exporting layers 6.1s done
#23 writing image sha256:675a0761a6e00a144457d5a7a70db1188d505d7679ab916f86f31135ed6882a1 done
#23 naming to docker.io/library/alopex-verify-release:0.8.7 done
#23 DONE 6.1s
```

### 2. verify-release-embedded ビルド ✅

公開検証用の2つの bin source を一時 crate へコピーし、ALOPEX_VERSION と完全一致する crates.io 公開版 alopex-embedded/alopex-core/alopex-sql だけを依存としてビルドする。固定 Cargo.toml の追随漏れと repository path 混入の双方を防ぐ。

```
  Downloaded thiserror-impl v2.0.20
  Downloaded thiserror v2.0.20
   Compiling thiserror v1.0.69
   Compiling thiserror-impl v1.0.69
   Compiling thiserror v2.0.20
   Compiling thiserror-impl v2.0.20
   Compiling arrow-cast v53.4.1
   Compiling ppv-lite86 v0.2.21
   Compiling ordered-float v2.10.1
   Compiling twox-hash v2.1.3
   Compiling byteorder v1.5.0
   Compiling integer-encoding v3.0.4
   Compiling thrift v0.17.0
   Compiling lz4_flex v0.11.6
   Compiling rand_chacha v0.3.1
   Compiling lexical-core v0.8.5
   Compiling alopex-sql v0.8.7
   Compiling arrow-select v52.2.0
   Compiling flate2 v1.1.9
   Compiling twox-hash v1.6.3
   Compiling tracing-attributes v0.1.31
   Compiling tracing-core v0.1.36
   Compiling getrandom v0.4.3
   Compiling pin-project-lite v0.2.17
   Compiling seq-macro v0.3.6
   Compiling tracing v0.1.44
   Compiling arrow-cast v52.2.0
   Compiling zstd v0.13.3
   Compiling rand v0.8.7
   Compiling bincode v1.3.3
   Compiling csv-core v0.1.13
   Compiling memmap2 v0.9.11
   Compiling equivalent v1.0.2
   Compiling hashbrown v0.17.1
   Compiling futures-core v0.3.34
   Compiling alopex-core v0.8.7
   Compiling indexmap v2.14.0
   Compiling csv v1.4.0
   Compiling arrow-ipc v52.2.0
   Compiling arrow-ipc v53.4.1
   Compiling brotli v6.0.0
   Compiling rmp v0.8.15
   Compiling lazy_static v1.5.0
   Compiling rmp-serde v1.3.1
   Compiling arrow-csv v53.4.1
   Compiling parquet v52.2.0
   Compiling uuid v1.24.1
   Compiling arrow-json v53.4.1
   Compiling md-5 v0.10.6
   Compiling brotli v7.0.0
   Compiling arrow-string v53.4.1
   Compiling arrow-ord v53.4.1
   Compiling arrow-row v53.4.1
   Compiling arrow-arith v53.4.1
   Compiling hex v0.4.3
   Compiling arrow v53.4.1
   Compiling parquet v53.4.1
   Compiling alopex-embedded v0.8.7
   Compiling alopex-dataframe v0.8.7
   Compiling alopex-cluster v0.8.7
   Compiling alopex-release-verifier v0.0.0 (/tmp/tmp.uDftMyRmDl)
    Finished `release` profile [optimized] target(s) in 3m 40s
```

### 3. mode-parity 検証 (verify.py) ✅

「ライブラリ・組み込み・サーバー・gRPC・クラスタの各サーフェスが同一 SQL コーパスに対して同一結果を返す」ことを機械検証する。S2a(単一プロセス内での全ペア比較)・S2b(writer/reader を分けた永続化データの相互可搬性)・S2c(旧版データの全reader互換)を全件実行し、SKIPを許可しない。

```
  --- PASS=7 / FAIL=0 / SKIP=0 / ERROR=0
  --- PASS=15 / FAIL=0 / SKIP=0 / ERROR=0
  --- PASS=4 / FAIL=0 / SKIP=0 / ERROR=0
合計: PASS=26 / FAIL=0 / SKIP=0 / ERROR=0  -> exit 0
========================================================================
mode-parity 検証結果
========================================================================

[s2a]
  PASS   embedded(self-assert)
  PASS   embedded<->cli
  PASS   embedded<->http
  PASS   embedded<->grpc
  PASS   cli<->http
  PASS   cli<->grpc
  PASS   http<->grpc

[s2b]
  PASS   writer=embedded/reader=embedded
  PASS   writer=embedded/reader=cli
  PASS   writer=embedded/reader=http
  PASS   writer=embedded/reader=grpc
  PASS   writer=embedded/reader=cluster
  PASS   writer=cli/reader=embedded
  PASS   writer=cli/reader=cli
  PASS   writer=cli/reader=http
  PASS   writer=cli/reader=grpc
  PASS   writer=cli/reader=cluster
  PASS   writer=http/reader=embedded
  PASS   writer=http/reader=cli
  PASS   writer=http/reader=http
  PASS   writer=http/reader=grpc
  PASS   writer=http/reader=cluster

[s2c]
  PASS   v0.8.4/reader=embedded
  PASS   v0.8.4/reader=cli
  PASS   v0.8.4/reader=http
  PASS   v0.8.4/reader=grpc

========================================================================
========================================================================
```

### 4. mode-parity デモ (demo.py) ✅

上記の機械検証と同一のコーパスを使い、「One Engine, Four Forms」を人間向けに実演する。第1幕(ライブラリ/インメモリ)で実行した結果が、第2幕(組み込み/ファイル永続化)・第3幕(シングルノードサーバー、HTTP と gRPC の両方)・第4幕(サーバー停止後に CLI で再オープン)・第5幕(cluster-aware 単一メンバー)を通じて一貫することを確認する。

```
       {'category': 'game', 'cnt': 2, 'total_price': 27.5}
  sql> SELECT docs.id FROM docs ORDER BY vector_distance(docs.embedding, [1.0, 0.0, 0.0], 'l2') ASC LIMIT 3
       columns: id
       {'id': 1}
       {'id': 5}
       {'id': 2}
  ✔ 検証一致 (8 文)

========================================================================
第 5 幕 [SF-CLUSTER] クラスタ(cluster-aware、単一メンバー)— 第 4 幕のデータをそのまま開く
========================================================================
  補足: v0.7 の cluster-aware foundation(単一メンバー、分散実行なし)。
        マルチノードの分散実行は v0.8 以降の予約。
  cluster-aware server ready: http=http://127.0.0.1:56387

  -- cluster status (GET /api/admin/status の cluster フィールド) --
       mode               : cluster_aware
       node_id            : parity-node-1
       cluster_id         : parity-cluster
       advertised_endpoint: 127.0.0.1:56387
       role / lifecycle   : gateway / active
       membership         : source=chirps members=1
         - node_id=parity-node-1 derived_state=active
       degraded           : False
  ✔ mode == cluster_aware / degraded == false

  -- 第 4 幕までの全データが可視か検証 (99_verify.sql, HTTP) --
  sql> SELECT COUNT(*) AS user_count FROM users
       columns: user_count
       {'user_count': 6}
  sql> SELECT COUNT(*) AS order_count, SUM(amount) AS order_total, AVG(amount) AS order_avg FROM orders
       columns: order_count, order_total, order_avg
       {'order_count': 9, 'order_total': 351.0, 'order_avg': 39.0}
  sql> SELECT COUNT(*) AS product_count, MIN(price) AS min_price, MAX(price) AS max_price FROM products
       columns: product_count, min_price, max_price
       {'product_count': 5, 'min_price': 6.0, 'max_price': 20.0}
  sql> SELECT COUNT(*) AS region_count FROM regions
       columns: region_count
       {'region_count': 3}
  sql> SELECT COUNT(*) AS doc_count FROM docs
       columns: doc_count
       {'doc_count': 4}
  sql> SELECT COUNT(*) AS joined_orders, SUM(orders.amount) AS joined_total FROM users INNER JOIN orders ON users.id = orders.user_id
       columns: joined_orders, joined_total
       {'joined_orders': 8, 'joined_total': 305.5}
  sql> SELECT category, COUNT(*) AS cnt, SUM(price) AS total_price FROM products GROUP BY category ORDER BY category
       columns: category, cnt, total_price
       {'category': 'book', 'cnt': 3, 'total_price': 33.0}
       {'category': 'game', 'cnt': 2, 'total_price': 27.5}
  sql> SELECT docs.id FROM docs ORDER BY vector_distance(docs.embedding, [1.0, 0.0, 0.0], 'l2') ASC LIMIT 3
       columns: id
       {'id': 1}
       {'id': 5}
       {'id': 2}
  ✔ 検証一致 (8 文)
  server stopped

========================================================================
デモ完了: 第 1〜5 幕 PASS (One Engine, Four Forms + cluster-aware)
========================================================================
```

### 5. v0.7 機能デモ: demo_cluster.py ✅

cluster status API のクロスサーフェス実証(D2)。single_node/cluster_aware の両モードでの起動、membership の join/leave によるライフサイクル遷移、Chirps 不可時の degraded フォールバックを、HTTP と CLI の両方で観測し、フィールドが一致することを確認する。

```
  server ready: http://127.0.0.1:53665
  -- HTTP(正): mode / identity / membership が設定値と一致 --
  ✔ cluster.mode = 'cluster_aware'
  ✔ identity.node_id = 'v07-demo-node-1'
  ✔ identity.cluster_id = 'v07-demo-cluster'
  ✔ identity.advertised_endpoint = '127.0.0.1:0'
  ✔ identity.role(既定 gateway) = 'gateway'
  ✔ identity.lifecycle_state = 'active'
  ✔ cluster.degraded = False
  ✔ membership.members 件数(自ノードのみ) = 1
  ✔ membership.members[0].node_id = 'v07-demo-node-1'
  ✔ membership.source(既定 chirps) = 'chirps'
  membership.source = chirps
    - node_id=v07-demo-node-1 derived_state=active transition_reason=initialized
  -- CLI: `alopex server status` のフィールド射影一致 --
  ✔ 場 2: CLI 表示 10 フィールドが HTTP 射影と一致
     (Version, Cluster Schema, Cluster Mode, Node ID, Lifecycle, Degraded, Local Only, Future Distributed, Scatter/Gather, Diagnostics)

========================================================================
場 3: membership lifecycle: leave -> 観測 -> join -> 観測
========================================================================
  -- POST /api/admin/cluster/leave --
  ✔ leave 応答 action = 'leave'
  ✔ leave 応答 identity.lifecycle_state = 'leaving'
  -- leave 後の観測(HTTP 正 + CLI 射影) --
  ✔ status identity.lifecycle_state = 'leaving'
  membership.source = chirps
    - node_id=v07-demo-node-1 derived_state=leaving transition_reason=leave_requested
  ✔ CLI Lifecycle 列 = 'leaving'
  ✔ 場 3 (leave 後): CLI 表示 10 フィールドが HTTP 射影と一致
     (Version, Cluster Schema, Cluster Mode, Node ID, Lifecycle, Degraded, Local Only, Future Distributed, Scatter/Gather, Diagnostics)
  -- CLI `alopex server join`(サーフェスを替えて join) --
  ✔ CLI join 行 Action = 'join'
  ✔ CLI join 行 Lifecycle = 'active'
  -- join 後の観測(HTTP 正 + CLI 射影) --
  ✔ status identity.lifecycle_state = 'active'
  membership.source = chirps
    - node_id=v07-demo-node-1 derived_state=active transition_reason=join_completed
  ✔ CLI Lifecycle 列 = 'active'
  ✔ 場 3 (join 後): CLI 表示 10 フィールドが HTTP 射影と一致
     (Version, Cluster Schema, Cluster Mode, Node ID, Lifecycle, Degraded, Local Only, Future Distributed, Scatter/Gather, Diagnostics)
  server stopped

========================================================================
場 4: membership_source_available=false で起動(degraded フォールバック)
========================================================================
  server ready: http://127.0.0.1:48995
  ✔ cluster.mode(cluster_aware のまま) = 'cluster_aware'
  ✔ cluster.degraded = True
  ✔ diagnostics codes = ['chirps_unavailable']
     - [chirps_unavailable] Chirps membership input is unavailable; using single-node-equivalent routing fallback
       remediation: enable the Chirps adapter or fix membership service configuration before relying on cluster routing
  -- GET /api/admin/health も degraded を報告する --
  ✔ health.status = 'degraded'
  ✔ health.degraded = True
  server stopped

========================================================================
デモ完了: 場 1〜4 PASS(検証対象外の注記: issue #35 の Python アクセサ)
========================================================================
```

### 6. v0.7 機能デモ: demo_routing.py ✅

SQL 実行がすべてルーティング判定を経由し、その決定理由が診断として観測できることを実証する(D3)。v0.7 のライブ実行は local_only であり、分散が必要な操作は明示的に拒否されることを確認する。

```
v0.7 機能デモ D3: ルーティング透明性

========================================================================
場 1: cluster_aware サーバーへの SQL 実行と routing 診断の実測
========================================================================
  server ready: http://127.0.0.1:59521
  routing_capabilities = {'local_only': True, 'future_distributed_execution_required': True, 'scatter_gather_simulated': True}

  sql> CREATE TABLE routing_demo (id INTEGER, name TEXT)
     - decision=local_only reason=placement_absent plan_id=c4e9d851-08c2-4393-9fed-3ce98d32b33a:0 targets=0 excluded=0
  ✔   decision = 'local_only'
  ✔   reason = 'placement_absent' (許容: ['single_resolved_target', 'placement_absent'])

  sql> INSERT INTO routing_demo VALUES (1, 'ember')
     - decision=local_only reason=placement_absent plan_id=2a252105-ecd4-4e38-b053-d7b253baf68a:0 targets=0 excluded=0
  ✔   decision = 'local_only'
  ✔   reason = 'placement_absent' (許容: ['single_resolved_target', 'placement_absent'])

  sql> INSERT INTO routing_demo VALUES (2, 'moss')
     - decision=local_only reason=placement_absent plan_id=73796998-b903-4f16-9b1d-de2778bb059a:0 targets=0 excluded=0
  ✔   decision = 'local_only'
  ✔   reason = 'placement_absent' (許容: ['single_resolved_target', 'placement_absent'])

  sql> SELECT id, name FROM routing_demo ORDER BY id
     - decision=local_only reason=placement_absent plan_id=e3f479ec-84a9-4bfc-bedf-6532e19d6044:0 targets=0 excluded=0
  ✔   decision = 'local_only'
  ✔   reason = 'placement_absent' (許容: ['single_resolved_target', 'placement_absent'])
  ✔ SELECT の結果行数(ローカル実行の傍証) = 2
  server stopped
  注記: 分散必要時の拒否 (future_distributed_execution_required) は、本番サーフェスに placement を跨がせる操作用 API が存在しない(設計上の意図)ため、ライブでは再現しない。該当挙動は次のサーバー統合テストが担保する:
      - crates/alopex-server/tests/http_sql_e2e.rs::non_session_sql_rejects_future_distributed_routing
      - crates/alopex-server/tests/http_sql_e2e.rs::future_distributed_write_is_rejected_before_local_execution
  注記: 場 2 は repository source の simulated_harness unit test であり、公開パッケージのみを対象にする release verification には含めない。

========================================================================
デモ完了: 場 1 PASS / 場 2 N/A (source-only unit contract)
========================================================================
```

### 7. v0.7 機能デモ: demo_dataframe_p3.py ✅

DataFrame の string/datetime/list 名前空間関数と explode/implode の往復変換が Rust と Python の両サーフェスで決定的に(同一入力に対して常にバイト単位で同一の出力を)動作することを確認する(D4)。

```
     ])?
  ✔ dt.ts = [0, 1711927800000000, None]
  ✔ dt.year = [1970, 2024, None]
  ✔ dt.month = [1, 3, None]
  ✔ dt.weekday = [4, 7, None]
  ✔ dt.tokyo = [32400000000, 1711960200000000, None]

========================================================================
場 3: list namespace: join / len / contains -> explode -> implode 往復
========================================================================
  入力: {'id': ['r1', 'r2', 'r3', 'r4'], 'tags': ['red,green', 'solo', None, 'blue,']}
  -- 呼び出し構文 (Python) --
     listified = alopex.DataFrame({...}).str("tags").split(",", "parts")
     listified.list("parts").join("|", "NULL", "joined")
              .list("parts").len("n")
              .list("parts").contains("red", "has_red")
     with_ops.explode("parts")     # リスト列を行へ展開
     nested.explode("items").implode()   # explode -> implode の往復
  -- 呼び出し構文 (Rust) --
     let listified = df.with_columns(vec![
         col("tags").str().split(",").alias("parts"),
     ])?;
     listified.with_columns(vec![
         col("parts").list().join("|", Some("NULL")).alias("joined"),
         col("parts").list().len().alias("n"),
         col("parts").list().contains("red").alias("has_red"),
     ])?
     df.explode("parts")?          // DataFrame::explode(column)
     df.explode("items")?.implode()?   // explode -> implode の往復
  -- list.join / len / contains --
  ✔ list_ops.id = ['r1', 'r2', 'r3', 'r4']
  ✔ list_ops.tags = ['red,green', 'solo', None, 'blue,']
  ✔ list_ops.parts = [['red', 'green'], ['solo'], None, ['blue', '']]
  ✔ list_ops.joined = ['red|green', 'solo', None, 'blue|']
  ✔ list_ops.n = [2, 1, None, 2]
  ✔ list_ops.has_red = [True, False, None, False]
  -- explode(空リスト・null リストは 1 行の null、他列は複製) --
  ✔ exploded.id = ['r1', 'r1', 'r2', 'r3', 'r4', 'r4']
  ✔ exploded.tags = ['red,green', 'red,green', 'solo', None, 'blue,', 'blue,']
  ✔ exploded.parts = ['red', 'green', 'solo', None, 'blue', '']
  ✔ exploded.joined = ['red|green', 'red|green', 'solo', None, 'blue|', 'blue|']
  ✔ exploded.n = [2, 2, 1, None, 2, 2]
  ✔ exploded.has_red = [True, True, False, None, False, False]
  -- explode → implode の往復が元と等価 --
  ✔ explode(nested).items = ['a', 'b', 'c']
  ✔ implode(explode(nested)).items(元と等価) = [['a', 'b', 'c']]
  -- implode → explode の往復(null 要素の保存) --
  ✔ implode(words).word = [['ember', None, 'moss']]
  ✔ explode(implode(words)).word(元と等価) = ['ember', None, 'moss']

========================================================================
場 4: 決定性: 同一入力で 2 回実行し、全出力がバイト単位で一致
========================================================================
  1 回目: 1231 bytes sha256=6afefaf607eefe425d4c25c06b45b2782f4345bc1a05db5ae32406c72ccfa151
  2 回目: 1231 bytes sha256=6afefaf607eefe425d4c25c06b45b2782f4345bc1a05db5ae32406c72ccfa151
  ✔ 全出力がバイト単位で一致

========================================================================
デモ完了: 場 1〜4 PASS
========================================================================
```

### 8. v0.8.7 回帰保証: alopex-sql aggregate state/distinct/parallel ✅

crates.io 公開版 alopex-sql だけを依存にした一時 Rust crate をコンテナ内で作成し、Accumulator state/merge、COUNT 以外の DISTINCT 集約、単一プロセス partial→final parallel aggregate が v0.8.7 で実際に動作することを確認する。リポジトリ内の alopex 製品ソースはビルドしないため、公開 artifact の振る舞い保証になる。

```
aggregate regression guarantee passed
```

### 9. v0.8.7 SQL scalar/PRAGMA 動作保証 ✅

crates.io/PyPI から取得した v0.8.7 の CLI で、ハッシュ・UUID・エンコード・文字列関数と PRAGMA の公開利用経路を確認する。ソースの cargo build は行わず、インストール済みの alopex CLI だけを実行する。

```
╞════════════╪═════════════╪══════════╡
│ ALOPEX     ┆ 3           ┆ ok       │
└────────────┴─────────────┴──────────┘
== Numeric, regex, and pattern functions ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌──────────┬──────────┬──────────┬───────┬───────┐
│ absolute ┆ rounded  ┆ root     ┆ match ┆ col_4 │
╞══════════╪══════════╪══════════╪═══════╪═══════╡
│ 3        ┆ 1.230000 ┆ 3.000000 ┆ 42    ┆ true  │
└──────────┴──────────┴──────────┴───────┴───────┘
== SQL standard string forms ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌─────────────────┬────────────────┐
│ substring_value ┆ position_value │
╞═════════════════╪════════════════╡
│ lop             ┆ 4              │
└─────────────────┴────────────────┘
== Memory statistics function ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌──────────────┐
│ memory_stats │
╞══════════════╡
│ NULL         │
└──────────────┘
== I/O statistics function ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ io_stats                                                                                                                                              │
╞═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╡
│ {"wal_write_bytes":0,"sstable_read_bytes":0,"buffer_pool_hit_rate":1,"buffer_pool_size_bytes":0,"memtable_size_bytes":0,"compaction_bytes_written":0} │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
== Cache control function ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌─────────────┐
│ clear_cache │
╞═════════════╡
│ 0           │
└─────────────┘
== PRAGMA controls ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌────────┬──────────────────────────────────┐
│ status ┆ message                          │
╞════════╪══════════════════════════════════╡
│ OK     ┆ Operation completed successfully │
└────────┴──────────────────────────────────┘
== PRAGMA memory limit ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌────────┬──────────────────────────────────┐
│ status ┆ message                          │
╞════════╪══════════════════════════════════╡
│ OK     ┆ Operation completed successfully │
└────────┴──────────────────────────────────┘
== PRAGMA I/O stats ==
Warning: no TTY detected, default TUI is disabled, falling back to batch output.
┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ io_stats                                                                                                                                              │
╞═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╡
│ {"wal_write_bytes":0,"sstable_read_bytes":0,"buffer_pool_hit_rate":1,"buffer_pool_size_bytes":0,"memtable_size_bytes":0,"compaction_bytes_written":0} │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
v0.7.4 SQL scalar and PRAGMA demo completed.
```

### 10. v0.8.7 v0.8 SQL correctness incl. ROWS/RANGE frames (demo_sql_v08.py) ✅

PyPI 公開版で、v0.8 系の TIMESTAMP 書込み、数値型昇格、SUM(INTEGER)、CAST、IN/BETWEEN、異種数値 JOIN、重複 range-variable 拒否を実行し、値とエラー型を確認する。

```
   -> ERROR PyAlopexError: error[ALOPEX-C004]: ambiguous column 'id' found in tables: ["x", "x"] at line 1, column 46
   -> ERROR PyAlopexError: error[ALOPEX-C003]: column 'ident' not found in table 'sales' at line 1, column 41
   -> ERROR PyAlopexError: error[ALOPEX-C003]: column 'area' not found in table 'sales' at line 1, column 53
   -> ERROR PyAlopexError: error[ALOPEX-T001]: type mismatch: expected Integer, found Text at line 1, column 35
   -> ERROR PyAlopexError: error[ALOPEX-T008]: set operation column count mismatch: left 1, right 2 at line 1, column 22
   -> ERROR PyAlopexError: error[ALOPEX-T001]: type mismatch: expected Integer, found Text at line 1, column 22
   -> ERROR PyAlopexError: error[ALOPEX-C001]: table 'missing' not found at line 1, column 49
   -> [{'id': 1, 'grand': 650.0}, {'id': 2, 'grand': 650.0}, {'id': 3, 'grand': 650.0}, {'id': 4, 'grand': 650.0}, {'id': 5, 'grand': 650.0}]
call> db.execute_sql('SELECT id, SUM(amount) OVER (PARTITION BY region) AS regional FROM sales ORDER BY id')
   -> [{'id': 1, 'regional': 300.0}, {'id': 2, 'regional': 300.0}, {'id': 3, 'regional': 300.0}, {'id': 4, 'regional': 300.0}, {'id': 5, 'regional': 50.0}]
call> db.execute_sql('SELECT id, SUM(amount) OVER (ORDER BY id) AS running FROM sales ORDER BY id')
   -> [{'id': 1, 'running': 100.0}, {'id': 2, 'running': 300.0}, {'id': 3, 'running': 450.0}, {'id': 4, 'running': 600.0}, {'id': 5, 'running': 650.0}]
call> db.execute_sql('SELECT id, ROW_NUMBER() OVER (PARTITION BY region ORDER BY amount DESC, id) AS rn FROM sales ORDER BY id')
   -> [{'id': 1, 'rn': 2}, {'id': 2, 'rn': 1}, {'id': 3, 'rn': 1}, {'id': 4, 'rn': 2}, {'id': 5, 'rn': 1}]
call> db.execute_sql('SELECT id, RANK() OVER (ORDER BY amount) AS rank_value, DENSE_RANK() OVER (ORDER BY amount) AS dense_value FROM sales ORDER BY id')
   -> [{'id': 1, 'rank_value': 2, 'dense_value': 2}, {'id': 2, 'rank_value': 5, 'dense_value': 4}, {'id': 3, 'rank_value': 3, 'dense_value': 3}, {'id': 4, 'rank_value': 3, 'dense_value': 3}, {'id': 5, 'rank_value': 1, 'dense_value': 1}]
call> db.execute_sql('SELECT id, COUNT(*) OVER (PARTITION BY region) AS region_count, AVG(amount) OVER (PARTITION BY region) AS region_mean FROM sales ORDER BY id')
   -> [{'id': 1, 'region_count': 2, 'region_mean': 150.0}, {'id': 2, 'region_count': 2, 'region_mean': 150.0}, {'id': 3, 'region_count': 2, 'region_mean': 150.0}, {'id': 4, 'region_count': 2, 'region_mean': 150.0}, {'id': 5, 'region_count': 1, 'region_mean': 50.0}]
call> db.execute_sql('SELECT id, SUM(bonus) OVER (PARTITION BY region) AS region_bonus FROM sales ORDER BY id')
   -> [{'id': 1, 'region_bonus': 10.0}, {'id': 2, 'region_bonus': 10.0}, {'id': 3, 'region_bonus': 20.0}, {'id': 4, 'region_bonus': 20.0}, {'id': 5, 'region_bonus': 5.0}]
call> db.execute_sql('SELECT region, ROW_NUMBER() OVER (PARTITION BY region ORDER BY id) AS rn FROM sales ORDER BY region, rn')
   -> [{'region': 'east', 'rn': 1}, {'region': 'east', 'rn': 2}, {'region': 'north', 'rn': 1}, {'region': 'west', 'rn': 1}, {'region': 'west', 'rn': 2}]
call> db.execute_sql('SELECT DISTINCT SUM(amount) AS total, DENSE_RANK() OVER (ORDER BY SUM(amount)) AS sales_rank, SUM(SUM(amount)) OVER () AS grand FROM sales GROUP BY region HAVING SUM(amount) >= 50 ORDER BY total')
   -> [{'total': 50.0, 'sales_rank': 1, 'grand': 650.0}, {'total': 300.0, 'sales_rank': 2, 'grand': 650.0}]
call> db.execute_sql('SELECT id, LAG(amount) OVER (ORDER BY id) AS previous, LEAD(amount, 2, -1) OVER (ORDER BY id) AS two_ahead, LAG(amount, 0) OVER (ORDER BY id) AS current_value, amount - LAG(amount, 1, amount) OVER (ORDER BY id) AS delta FROM sales ORDER BY id')
   -> [{'id': 1, 'previous': None, 'two_ahead': 150.0, 'current_value': 100.0, 'delta': 0.0}, {'id': 2, 'previous': 100.0, 'two_ahead': 150.0, 'current_value': 200.0, 'delta': 100.0}, {'id': 3, 'previous': 200.0, 'two_ahead': 50.0, 'current_value': 150.0, 'delta': -50.0}, {'id': 4, 'previous': 150.0, 'two_ahead': -1.0, 'current_value': 150.0, 'delta': 0.0}, {'id': 5, 'previous': 150.0, 'two_ahead': -1.0, 'current_value': 50.0, 'delta': -100.0}]
call> db.execute_sql('SELECT id, LAG(amount, 1, -1) OVER (PARTITION BY region ORDER BY id) AS previous, LEAD(bonus, 1, -1) OVER (PARTITION BY region ORDER BY id) AS following_bonus FROM sales ORDER BY id')
   -> [{'id': 1, 'previous': -1.0, 'following_bonus': None}, {'id': 2, 'previous': 100.0, 'following_bonus': -1.0}, {'id': 3, 'previous': -1.0, 'following_bonus': None}, {'id': 4, 'previous': 150.0, 'following_bonus': -1.0}, {'id': 5, 'previous': -1.0, 'following_bonus': -1.0}]
call> db.execute_sql('SELECT id, SUM(qty) OVER (ORDER BY id ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS physical FROM sales ORDER BY id')
   -> [{'id': 1, 'physical': 4}, {'id': 2, 'physical': 9}, {'id': 3, 'physical': 8}, {'id': 4, 'physical': 7}, {'id': 5, 'physical': 2}]
call> db.execute_sql('SELECT id, SUM(qty) OVER (ORDER BY amount RANGE BETWEEN 50 PRECEDING AND CURRENT ROW) AS value_frame FROM sales ORDER BY id')
   -> [{'id': 1, 'value_frame': 3}, {'id': 2, 'value_frame': 8}, {'id': 3, 'value_frame': 10}, {'id': 4, 'value_frame': 10}, {'id': 5, 'value_frame': 0}]
call> db.execute_sql("SELECT region FROM sales WHERE region = 'west' UNION SELECT region FROM sales WHERE region = 'north'")
   -> [{'region': 'west'}, {'region': 'north'}]
call> db.execute_sql('SELECT id FROM sales WHERE amount >= 150 UNION SELECT id FROM sales WHERE qty <= 2')
   -> [{'id': 2}, {'id': 3}, {'id': 4}, {'id': 5}]
call> db.execute_sql('SELECT id FROM sales WHERE amount >= 150 UNION ALL SELECT id FROM sales WHERE qty <= 2')
   -> [{'id': 2}, {'id': 3}, {'id': 4}, {'id': 2}, {'id': 4}, {'id': 5}]
call> db.execute_sql('SELECT id FROM sales WHERE amount >= 150 INTERSECT SELECT id FROM sales WHERE qty <= 2')
   -> [{'id': 2}, {'id': 4}]
call> db.execute_sql('SELECT id FROM sales WHERE amount >= 150 EXCEPT SELECT id FROM sales WHERE qty <= 2')
   -> [{'id': 3}]
call> db.execute_sql('SELECT id FROM sales WHERE qty <= 2 EXCEPT SELECT id FROM sales WHERE amount >= 150')
   -> [{'id': 5}]
call> db.execute_sql('SELECT 1 AS value UNION ALL SELECT 1 UNION SELECT 1')
   -> [{'value': 1}]
call> db.execute_sql('SELECT bonus FROM sales UNION SELECT bonus FROM sales')
   -> [{'bonus': 10.0}, {'bonus': None}, {'bonus': 20.0}, {'bonus': 5.0}]
call> db.execute_sql('SELECT 1 AS value UNION SELECT 1 UNION ALL SELECT 1')
   -> [{'value': 1}, {'value': 1}]
call> db.execute_sql('SELECT 1 AS value UNION SELECT 2 INTERSECT SELECT 2')
   -> [{'value': 1}, {'value': 2}]
call> db.execute_sql('SELECT * FROM ints AS x JOIN doubles AS x ON x.id = x.id')
call> db.execute_sql('SELECT amount AS ident FROM sales WHERE ident > 100')
call> db.execute_sql('SELECT region AS area, COUNT(*) FROM sales GROUP BY area')
call> db.execute_sql("SELECT CASE WHEN TRUE THEN 1 ELSE 'text' END")
call> db.execute_sql('SELECT id FROM sales UNION SELECT id, region FROM sales')
call> db.execute_sql('SELECT id FROM sales UNION SELECT region FROM sales')
call> db.execute_sql('WITH defined AS (SELECT 1 AS id) SELECT id FROM missing')
v0.8 SQL correctness demo completed: 55 checks passed
```

### 11. v0.8.7 組み込み API サーフェス (demo_api_surfaces.py) ✅

PyPI 公開版の Python バインディングから SQL を実行する経路を実演する。Database.new()(SF-MEM)/ Database.open(path)(SF-FILE)でのコーパス実行と再オープン、Transaction の commit/rollback、execute_sql_stream() の反復取得、統計関数と PRAGMA を Python から実行する。最後に CLI/HTTP/gRPC/Rust API/Python API の 5 経路が同一コーパスに対して同一の正規化結果を返すことを表示する。従来の mode-parity(4 経路)に Python API を加えた確認である。

```
            {'io_stats': '{"wal_write_bytes":0,"sstable_read_bytes":0,"buffer_pool_hit_rate":1,"buffer_pool_size_bytes":0,"memtable_size_bytes":21052,"compaction_bytes_written":0}'}
  注記: demo_sql_v074.sh は同じ関数・PRAGMA を CLI から実行している。 ここでは同一のものを Python の execute_sql() から実行した。

========================================================================
場 6: 5 経路の等価性 — CLI / HTTP / gRPC / Rust API / Python API
========================================================================
  注記: 従来の mode-parity は CLI・HTTP・gRPC・Rust(embedded)の 4 経路。 ここに Python API を加えた 5 経路で、同一コーパスの正規化後の結果が 一致するかを表示する(デモのため不一致でも exit は落とさない)。

  -- Rust 組み込み API (alopex-embedded) --
  call> EmbeddedSurface(repo).run(corpus, role='writer', data_dir=None)  # PARITY_ROLE=writer / PARITY_DATA_DIR 未設定 = インメモリ
       -> 95 文の正規化出力を取得

  -- CLI (alopex --batch --output json) --
  call> CliSurface.run_statements(<コーパス>, data_dir=...)
  call> CliSurface.run_statements(<検証クエリ>, data_dir=...)
       sql: SELECT COUNT(*) AS user_count FROM users
            columns: user_count
            {'user_count': 6}
       sql: SELECT COUNT(*) AS order_count, SUM(amount) AS order_total, AVG(amount) AS order_avg FROM orders
            columns: order_count, order_total, order_avg
            {'order_count': 7, 'order_total': 294.0, 'order_avg': 42.0}
       sql: SELECT COUNT(*) AS product_count, MIN(price) AS min_price, MAX(price) AS max_price FROM products
            columns: product_count, min_price, max_price
            {'product_count': 5, 'min_price': 6.0, 'max_price': 20.0}
       sql: SELECT COUNT(*) AS region_count FROM regions
            columns: region_count
            {'region_count': 3}
       sql: SELECT COUNT(*) AS doc_count FROM docs
            columns: doc_count
            {'doc_count': 4}
       sql: SELECT COUNT(*) AS joined_orders, SUM(orders.amount) AS joined_total FROM users INNER JOIN orders ON users.id = orders.user_id
            columns: joined_orders, joined_total
            {'joined_orders': 6, 'joined_total': 248.5}
       sql: SELECT category, COUNT(*) AS cnt, SUM(price) AS total_price FROM products GROUP BY category ORDER BY category
            columns: category, cnt, total_price
            {'category': 'book', 'cnt': 3, 'total_price': 33.0}
            {'category': 'game', 'cnt': 2, 'total_price': 27.5}
       sql: SELECT docs.id FROM docs ORDER BY vector_distance(docs.embedding, [1.0, 0.0, 0.0], 'l2') ASC LIMIT 3
            columns: id
            {'id': 1}
            {'id': 5}
            {'id': 2}

  -- HTTP (http://127.0.0.1:33077) --
  call> HttpSurface(base).run_statements(<検証クエリ>)
  -- gRPC (127.0.0.1:34103) --
  call> GrpcSurface(target).run_statements(<検証クエリ>)

  -- 検証クエリ 8 文の一致(基準: CLI)--
  ✔ CLI <-> HTTP: 全 8 文が一致
  ✔ CLI <-> gRPC: 全 8 文が一致
  ✔ CLI <-> Python API: 全 8 文が一致

  -- Rust API はコーパス実行そのものの出力(検証クエリとは別)--
     Rust API entries = 95 文
  注記: Rust(embedded)経路はコーパス 01〜07 を通し実行した結果を返す契約(EmbeddedSurface)であり、検証クエリ 8 文とは対象が異なる。 検証クエリでの 5 経路一致は verify.py (S2-a/S2-b) が担う。

========================================================================
デモ完了: 場 1〜6
========================================================================
```

### 12. v0.8.7 ベクトル検索 API (demo_vector_api.py) ✅

PyPI 公開版の Python バインディングから、SQL 経由とネイティブ API の両方でベクトル検索を実行する。API 不在時だけ issue #82 を明記して SKIP とし、存在時は全メソッドを呼び出して L2 距離と node_count を表示する。

```
ベクトル検索 API デモ (Rust / Python)

========================================================================
場 1: Python 組み込み API — SQL 経由のベクトル検索
========================================================================
  call> alopex.Database.new()
  call> db.execute_sql('CREATE TABLE docs (id INT PRIMARY KEY, title TEXT, embedding VECTOR(3, L2))')
       -> None
  call> db.execute_sql(<docs 4 行の INSERT>)
       -> 4 row(s) affected
  call> db.execute_sql("SELECT docs.id, vector_distance(docs.embedding, [1.0, 0.0, 0.0], 'l2') AS dist FROM docs ORDER BY vector_distance(docs.embedding, [1.0, 0.0, 0.0], 'l2') ASC LIMIT 3")
       {'id': 1, 'dist': 0.0}
       {'id': 5, 'dist': 0.25}
       {'id': 2, 'dist': 1.1180340051651}
  注記: docs は scripts/parity/corpus と同一(99_verify.sql 時点で 4 行)。 クエリ点 [1.0, 0.0, 0.0] からの L2 距離昇順で Top-3 を取得する。

========================================================================
場 2: Python 組み込み API — ネイティブベクトル API
========================================================================
  call> db.create_hnsw_index('idx_docs_embedding', alopex.HnswConfig(3))
  call> db.begin(alopex.TxnMode.READ_WRITE)
  call> tx.upsert_vector(b'flat-quarter', None, quarter, alopex.Metric.L2)
  call> tx.search_similar(query, alopex.Metric.L2, 1)
       <builtins.SearchResult object at 0x7fbcdaac15f0>
  call> tx.get_vector(b'flat-quarter', alopex.Metric.L2)
       [0.75, 0.0, 0.0]
  call> tx.upsert_to_hnsw('idx_docs_embedding', b'point', query)
  call> tx.upsert_to_hnsw('idx_docs_embedding', b'quarter', quarter)
  call> tx.commit()
  call> db.search_hnsw('idx_docs_embedding', query, 2)
       <builtins.SearchResult object at 0x7fbcdaac15f0>
       <builtins.SearchResult object at 0x7fbcdaac1650>
       node_count=2
  call> db.get_hnsw_stats('idx_docs_embedding')
       node_count=2
  call> tx.delete_from_hnsw('idx_docs_embedding', b'quarter')
  call> db.drop_hnsw_index('idx_docs_embedding')

========================================================================
デモ完了: 場 1 実行 / 場 2 実行
========================================================================
```

### 13. v0.8.7 Embedded API 全シナリオ ✅

crates.io 公開版 alopex-embedded/alopex-core/alopex-sql だけでビルドした専用バイナリを使い、保存・KV/transaction・local SQL 全カテゴリ・catalog/cluster 診断・owned/SQL stream・DataFrame/columnar・Vector/HNSW・large value・fail-closed 境界の10シナリオを Rust Embedded API から自己検証付きで実演する。外部 cluster、Python 専用 API、未 provision の V08 segment、default feature 外 S3 は成功に偽装せず明示的な境界として確認する。

```
=== EMB-10-fail-closed-boundaries: unsupported and external-prerequisite boundaries ===
PASS EMB-10-fail-closed-boundaries
PASS EMB-02-kv-transactions

=== EMB-03-persisted-transaction-manager: persisted transaction manager ===
PASS EMB-03-persisted-transaction-manager

=== EMB-04-local-sql-matrix: complete inherited local SQL matrix ===
SQL rows=2 :: SELECT DISTINCT dept_id FROM metrics WHERE n IN (2, 3, 4) AND n BETWEEN 2 AND 4 ORDER BY dept_id LIMIT 2 OFFSET 0
SQL rows=2 :: SELECT d.name, SUM(m.n) AS total, AVG(m.n) AS mean FROM departments AS d JOIN metrics AS m ON d.id = m.dept_id GROUP BY d.name HAVING SUM(m.n) >= 4 ORDER BY d.name
SQL rows=1 :: SELECT id FROM metrics WHERE n > (SELECT AVG(n) FROM metrics) ORDER BY id
SQL rows=1 :: SELECT CAST(n AS DOUBLE) AS converted, n * 2.0 AS doubled FROM metrics WHERE id = 1
SQL rows=1 :: SELECT md5('abc') AS digest, hex(unhex('A1B2')) AS roundtrip, upper('alopex') AS name
SQL rows=1 :: SELECT SUM(n) AS integer_sum, COUNT(DISTINCT dept_id) AS departments FROM metrics
SQL rows=2 :: SELECT id, vector_distance(embedding, [1.0, 0.0, 0.0], 'l2') AS distance FROM metrics ORDER BY vector_distance(embedding, [1.0, 0.0, 0.0], 'l2') LIMIT 2
SQL rows=1 :: SELECT ts FROM metrics WHERE id = 1
SQL rows=5 :: SELECT amount AS id FROM sales ORDER BY id
SQL rows=2 :: SELECT region, SUM(amount) AS total FROM sales GROUP BY region HAVING total >= 300 ORDER BY region
SQL rows=1 :: SELECT amount, pg_typeof(amount) AS kind FROM sales WHERE id = 1
SQL rows=4 :: SELECT id FROM sales WHERE amount >= 150 UNION SELECT id FROM sales WHERE qty <= 2 ORDER BY id
SQL rows=6 :: SELECT id FROM sales WHERE amount >= 150 UNION ALL SELECT id FROM sales WHERE qty <= 2 ORDER BY id
SQL rows=2 :: SELECT id FROM sales WHERE amount >= 150 INTERSECT SELECT id FROM sales WHERE qty <= 2 ORDER BY id
SQL rows=1 :: SELECT id FROM sales WHERE qty <= 2 EXCEPT SELECT id FROM sales WHERE amount >= 150 ORDER BY id
SQL rows=5 :: SELECT id, CASE WHEN qty > 2 THEN 'bulk' ELSE 'small' END AS band, CASE WHEN bonus > 10 THEN 'large' END AS bonus_band, CASE WHEN qty = 0 THEN 1 ELSE 2.5 END AS numeric_case, CASE WHEN qty = 0 THEN TRUE ELSE FALSE END AS is_zero FROM sales ORDER BY id
SQL rows=6 :: WITH chosen AS (SELECT id, region FROM sales WHERE amount >= 150) SELECT sales.id AS sales_id, chosen.id AS chosen_id FROM sales JOIN chosen ON sales.region = chosen.region ORDER BY sales_id, chosen_id
SQL rows=1 :: WITH renamed(identifier, territory) AS (SELECT id, region FROM sales WHERE id = 1) SELECT territory, identifier FROM renamed
SQL rows=1 :: SELECT id FROM sales WHERE amount >= 150 EXCEPT SELECT id FROM sales WHERE qty <= 2 ORDER BY id
SQL rows=1 :: WITH sales AS (SELECT id + 100 AS id FROM sales WHERE id = 1) SELECT id FROM sales
SQL rows=5 :: SELECT id, ROW_NUMBER() OVER (ORDER BY id) AS rn, RANK() OVER (ORDER BY amount) AS rank_value, DENSE_RANK() OVER (ORDER BY amount) AS dense_value, SUM(amount) OVER (ORDER BY id) AS running FROM sales ORDER BY id
SQL rows=2 :: SELECT DISTINCT SUM(amount) AS total, DENSE_RANK() OVER (ORDER BY SUM(amount)) AS sales_rank, SUM(SUM(amount)) OVER () AS grand FROM sales GROUP BY region HAVING SUM(amount) >= 50 ORDER BY total
SQL rows=5 :: SELECT id, LAG(amount) OVER (ORDER BY id) AS previous, LEAD(amount, 2, -1) OVER (ORDER BY id) AS two_ahead, LAG(amount, 0) OVER (ORDER BY id) AS current_value, amount - LAG(amount, 1, amount) OVER (ORDER BY id) AS delta FROM sales ORDER BY id
SQL rows=5 :: SELECT id, LAG(amount, 1, -1) OVER (PARTITION BY region ORDER BY id) AS previous, LEAD(bonus, 1, -1) OVER (PARTITION BY region ORDER BY id) AS following_bonus FROM sales ORDER BY id
SQL rows=5 :: SELECT id, SUM(qty) OVER (ORDER BY id ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS physical FROM sales ORDER BY id
SQL rows=5 :: SELECT id, SUM(qty) OVER (ORDER BY amount RANGE BETWEEN 50 PRECEDING AND CURRENT ROW) AS value_frame FROM sales ORDER BY id
SQL rejected as expected (ALOPEX-C003) :: SELECT amount AS ident FROM sales WHERE ident > 100
SQL rejected as expected (ALOPEX-C003) :: SELECT region AS area, COUNT(*) FROM sales GROUP BY area
PASS EMB-04-local-sql-matrix

=== EMB-05-catalog-cluster-diagnostics: catalog and single-node cluster diagnostics ===
cluster status: ClusterStatusSnapshot { schema_version: 1, mode: SingleNode, identity: ClusterIdentity { node_id: NodeId("local"), cluster_id: None, advertised_endpoint: None, role: Gateway, lifecycle_state: Unconfigured, metadata_schema_version: 1, update_epoch: 9 }, membership: MembershipView { schema_version: 1, update_epoch: 9, source: LocalDefault, members: [] }, placement: PlacementView { schema_version: 1, update_epoch: 9, placements: [] }, routing_capabilities: RoutingCapabilities { local_only: true, future_distributed_execution_required: true, scatter_gather_simulated: true }, metrics_summary: ClusterMetricsSummary { source: LiveStatusSurface, members: [] }, degraded: false, diagnostics: [] }
routing diagnostics: RoutingDiagnostics { schema_version: 1, update_epoch: 9, decision: LocalOnly, reason: PlacementAbsent, plan_id: PlanId("embedded.statement.0"), roles: [], targets: [], excluded_targets: [], retry_summary: None }
PASS EMB-05-catalog-cluster-diagnostics

=== EMB-06-owned-and-sql-streams: owned sessions and SQL streams ===
PASS EMB-06-owned-and-sql-streams

=== EMB-07-dataframe-columnar: DataFrame and columnar APIs ===
DataFrame schema: Schema { fields: [Field { name: "id", data_type: Int32, nullable: true, dict_id: 0, dict_is_ordered: false, metadata: {} }, Field { name: "n", data_type: Int32, nullable: true, dict_id: 0, dict_is_ordered: false, metadata: {} }, Field { name: "f", data_type: Float32, nullable: true, dict_id: 0, dict_is_ordered: false, metadata: {} }, Field { name: "ts", data_type: Timestamp(Microsecond, None), nullable: true, dict_id: 0, dict_is_ordered: false, metadata: {} }], metadata: {} }
PASS EMB-07-dataframe-columnar

=== EMB-08-vector-hnsw: Vector and HNSW APIs ===
HNSW results: [HnswSearchResult { key: [111, 114, 105, 103, 105, 110], distance: 0.0, metadata: [122, 101, 114, 111] }, HnswSearchResult { key: [110, 101, 97, 114], distance: 1.0, metadata: [111, 110, 101] }, HnswSearchResult { key: [102, 97, 114], distance: 5.0, metadata: [102, 105, 118, 101] }]; search stats: SearchStats { nodes_visited: 5, distance_computations: 5, search_time_us: 3 }
HNSW index stats: HnswStats { node_count: 3, deleted_count: 0, level_distribution: [1, 2], memory_bytes: 544, avg_edges_per_node: 2.6666666666666665 }
PASS EMB-08-vector-hnsw

=== EMB-09-large-values: chunked large values ===
PASS EMB-09-large-values


All 10 embedded-local scenarios passed.
```

---

## 検証環境

| 項目 | 値 |
|---|---|
| 対象バージョン | v0.8.7 |
| 生成日時 (UTC) | 2026-08-18T15:28:48.824886Z |
| パッケージ取得元 | crates.io / PyPI |
| ソースビルド | なし(公開パッケージのみ使用) |
| Rust | `1.96` |
| Nim(ビルド専用イメージ) | `nimlang/nim:2.2` |
| Python | `3.11` |
